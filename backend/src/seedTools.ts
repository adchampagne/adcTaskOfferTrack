import { v4 as uuidv4 } from 'uuid';
import db from './database';

console.log('📚 Добавляем инструкции по инструментам в базу знаний...');

// Получаем ID админа
const admin = db.prepare('SELECT id FROM users WHERE role = ?').get('admin') as { id: string } | undefined;
const adminId = admin?.id || null;

// Проверяем, есть ли уже категория "Инструменты"
let toolsCategory = db.prepare(
  "SELECT id FROM knowledge_categories WHERE title = ? AND department_code = 'general'"
).get('Инструменты') as { id: string } | undefined;

if (!toolsCategory) {
  const categoryId = uuidv4();
  db.prepare(`
    INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order)
    VALUES (?, 'general', 'Инструменты', 'Wrench', 1)
  `).run(categoryId);
  toolsCategory = { id: categoryId };
  console.log('✅ Создана категория "Инструменты"');
} else {
  console.log('ℹ️ Категория "Инструменты" уже существует');
}

// Инструкции по инструментам
const instructions = [
  {
    title: 'Очистка метаданных',
    tags: ['метаданные', 'EXIF', 'GPS', 'приватность', 'фото', 'видео'],
    content: `# Очистка метаданных

Инструмент для удаления скрытой информации из медиафайлов.

## Что удаляется

- **EXIF данные** — дата съёмки, модель камеры/телефона
- **GPS координаты** — геолокация места съёмки
- **Информация о ПО** — какой программой редактировали
- **Авторские данные** — имя автора, комментарии, теги

## Поддерживаемые форматы

### Изображения
JPG, PNG, WebP, GIF, TIFF

### Видео
MP4, WebM, MOV, AVI, WMV, MPEG, 3GP, MKV

### Аудио
MP3, WAV, OGG, AAC, FLAC

## Как использовать

1. Перейдите в **Инструменты → Очистка метаданных**
2. Перетащите файлы в область загрузки или нажмите для выбора
3. Нажмите **"Очистить метаданные"**
4. Скачайте очищенные файлы

## Ограничения

- Максимальный размер файла: **500 МБ**
- Доступно для отделов: **Крео, Баинг**

## Зачем это нужно

При загрузке креативов в рекламные сети метаданные могут раскрыть:
- Ваше местоположение
- Устройство, с которого снимали
- Время создания контента

Очистка метаданных помогает сохранить анонимность.
`
  },
  {
    title: 'Конвертер изображений',
    tags: ['конвертер', 'изображения', 'WebP', 'JPG', 'PNG', 'сжатие'],
    content: `# Конвертер изображений

Конвертация и сжатие изображений прямо в браузере.

## Поддерживаемые форматы

### Входные
JPG, PNG, WebP, GIF, BMP

### Выходные
- **WebP** — современный формат, лучшее сжатие
- **JPG** — для фото, хорошее сжатие
- **PNG** — с прозрачностью, без потерь

## Настройки

### Качество (10-100%)
Чем ниже — тем меньше размер, но хуже качество.
Рекомендуемые значения:
- **85%** — оптимальный баланс
- **70%** — для веба, где важен размер
- **95%** — когда качество критично

### Изменение размера
Можно задать максимальную ширину и высоту.
Пропорции сохраняются автоматически.

## Как использовать

1. Перейдите в **Инструменты → Конвертер изображений**
2. Выберите формат и настройки качества
3. Перетащите изображения
4. Нажмите **"Конвертировать"**
5. Скачайте результат

## Зачем WebP

WebP даёт на **25-35% меньший размер** при том же качестве.
Поддерживается всеми современными браузерами.
Идеально для лендингов — быстрее загрузка = лучше конверсия.
`
  },
  {
    title: 'Генератор данных',
    tags: ['генератор', 'имена', 'телефоны', 'email', 'тестовые данные', 'гео'],
    content: `# Генератор данных

Создание реалистичных тестовых данных для разных стран.

## Что генерируется

- **Имена** — с учётом страны и пола
- **Телефоны** — в правильном формате для страны
- **Email** — реалистичные адреса

## Поддерживаемые страны

Более **200 стран** с локализованными данными:
- 🇺🇸 США, 🇬🇧 Великобритания, 🇩🇪 Германия
- 🇫🇷 Франция, 🇪🇸 Испания, 🇮🇹 Италия
- 🇧🇷 Бразилия, 🇲🇽 Мексика, 🇦🇷 Аргентина
- 🇯🇵 Япония, 🇰🇷 Корея, 🇮🇳 Индия
- И многие другие...

## Как использовать

1. Перейдите в **Инструменты → Генератор данных**
2. Выберите страну (или несколько)
3. Укажите количество записей
4. Выберите пол (или случайный)
5. Нажмите **"Сгенерировать"**
6. Скопируйте или экспортируйте данные

## Форматы экспорта

- **Копирование** — в буфер обмена
- **CSV** — для Excel
- **JSON** — для разработчиков

## Применение

- Тестирование форм на лендингах
- Заполнение CRM тестовыми данными
- Проверка валидации телефонов
- A/B тесты с разными гео
`
  },
  {
    title: 'Калькулятор ROI',
    tags: ['ROI', 'калькулятор', 'метрики', 'EPC', 'CPA', 'профит'],
    content: `# Калькулятор ROI

Расчёт ключевых метрик арбитража.

## Входные данные

| Поле | Описание |
|------|----------|
| **Расход (Spend)** | Сколько потратили на рекламу |
| **Доход (Revenue)** | Сколько получили с конверсий |
| **Клики** | Количество кликов по рекламе |
| **Конверсии** | Лиды или продажи |
| **Показы** | Опционально, для расчёта eCPM |

## Расчитываемые метрики

### ROI (Return on Investment)
\`\`\`
ROI = (Доход - Расход) / Расход × 100%
\`\`\`
**Пример:** Потратили $100, заработали $150 → ROI = 50%

### EPC (Earnings Per Click)
\`\`\`
EPC = Доход / Клики
\`\`\`
Сколько зарабатываете с одного клика.

### CPA (Cost Per Action)
\`\`\`
CPA = Расход / Конверсии
\`\`\`
Сколько стоит одна конверсия.

### CR (Conversion Rate)
\`\`\`
CR = Конверсии / Клики × 100%
\`\`\`
Процент кликов, ставших конверсиями.

### eCPC (Effective Cost Per Click)
\`\`\`
eCPC = Расход / Клики
\`\`\`
Фактическая стоимость клика.

### eCPM (Effective Cost Per Mille)
\`\`\`
eCPM = (Расход / Показы) × 1000
\`\`\`
Стоимость за 1000 показов.

## Точка безубыточности

Калькулятор также показывает:
- Минимальную выплату для ROI 0%
- Сколько нужно конверсий для ROI 100%
`
  },
  {
    title: 'UTM генератор',
    tags: ['UTM', 'ссылки', 'трекинг', 'аналитика', 'кампании'],
    content: `# UTM генератор

Создание ссылок с UTM-метками для отслеживания кампаний.

## UTM параметры

| Параметр | Описание | Пример |
|----------|----------|--------|
| **utm_source** | Источник трафика | facebook, google, tiktok |
| **utm_medium** | Тип трафика | cpc, cpm, social, email |
| **utm_campaign** | Название кампании | summer_sale, black_friday |
| **utm_term** | Ключевое слово | похудение, казино |
| **utm_content** | Вариант креатива | banner_1, video_2 |

## Быстрые шаблоны

Готовые пресеты для популярных источников:
- 📘 **Facebook Ads** — source: facebook, medium: cpc
- 🎵 **TikTok Ads** — source: tiktok, medium: cpc
- 🔍 **Google Ads** — source: google, medium: cpc
- 📷 **Instagram** — source: instagram, medium: social
- ✈️ **Telegram** — source: telegram, medium: social
- 🔔 **Push** — source: push, medium: push

## Дополнительные параметры

Можно добавить свои параметры:
- **sub1, sub2, sub3** — для трекера
- **clickid** — ID клика
- **campaign_id** — ID кампании

## Как использовать

1. Вставьте базовый URL
2. Выберите шаблон или заполните поля вручную
3. Добавьте дополнительные параметры если нужно
4. Скопируйте готовую ссылку

## Советы

- Используйте **snake_case** для названий: \`summer_sale\`, не \`Summer Sale\`
- utm_source и utm_medium — **обязательны** для корректной аналитики
- Добавляйте sub-параметры для детального трекинга
`
  },
  {
    title: 'Проверка редиректов',
    tags: ['редиректы', 'HTTP', 'дебаг', 'ссылки', 'клоака'],
    content: `# Проверка редиректов

Анализ цепочки редиректов для любого URL.

## Что показывает

- **Вся цепочка** — каждый редирект с URL
- **Статус-коды** — 200, 301, 302, 404 и др.
- **Время** — сколько занял каждый шаг
- **Финальный URL** — куда в итоге ведёт ссылка

## Статус-коды

| Код | Значение | Описание |
|-----|----------|----------|
| **200** | OK | Страница загружена |
| **301** | Permanent | Постоянный редирект |
| **302** | Temporary | Временный редирект |
| **307** | Temporary | Редирект с сохранением метода |
| **404** | Not Found | Страница не найдена |
| **500** | Server Error | Ошибка сервера |

## Применение

### Проверка клоаки
Убедитесь, что ссылка ведёт куда нужно для разных User-Agent.

### Дебаг трекера
Проверьте, что все редиректы работают корректно.

### Анализ конкурентов
Посмотрите, какие трекеры используют.

### Проверка коротких ссылок
Куда ведёт bit.ly или другой сокращатель.

## Как использовать

1. Вставьте URL в поле
2. Нажмите **"Проверить"**
3. Изучите цепочку редиректов
4. Скопируйте любой URL из цепочки

## Ограничения

- Максимум **20 редиректов** в цепочке
- Таймаут **10 секунд** на запрос
- Запросы идут от сервера (обход CORS)
`
  },
  {
    title: 'Часовые пояса',
    tags: ['время', 'часовые пояса', 'гео', 'планирование', 'расписание'],
    content: `# Часовые пояса

Конвертация времени между часовыми поясами.

## Зачем это нужно

При работе с разными гео важно понимать:
- Когда у аудитории **прайм-тайм**
- Когда запускать/останавливать кампании
- Когда планировать созвоны с партнёрами

## Популярные часовые пояса

### США
- 🇺🇸 **Нью-Йорк** (EST) — UTC-5
- 🇺🇸 **Лос-Анджелес** (PST) — UTC-8
- 🇺🇸 **Чикаго** (CST) — UTC-6

### Европа
- 🇬🇧 **Лондон** (GMT) — UTC+0
- 🇩🇪 **Берлин** (CET) — UTC+1
- 🇷🇺 **Москва** (MSK) — UTC+3

### Азия
- 🇯🇵 **Токио** — UTC+9
- 🇸🇬 **Сингапур** — UTC+8
- 🇮🇳 **Дели** — UTC+5:30

## Как использовать

1. Выберите **исходный часовой пояс**
2. Укажите **время и дату**
3. Добавьте нужные часовые пояса для сравнения
4. Смотрите время во всех зонах одновременно

## Лучшее время для рекламы

### США
- **9:00–21:00** по местному времени
- Избегайте ночи (23:00–6:00)
- Пик активности: 19:00–22:00

### Европа
- **8:00–22:00** по местному времени
- Обед (12:00–14:00) — хорошее время

### Общие советы
- **Будни vs выходные** — разные паттерны
- **Праздники** — трафик может упасть или вырасти
- Тестируйте разное время для вашей вертикали
`
  }
];

// Добавляем инструкции
const insertInstruction = db.prepare(`
  INSERT OR REPLACE INTO knowledge_instructions (id, category_id, title, content, tags, sort_order, created_by, updated_by)
  VALUES (?, ?, ?, ?, ?, ?, ?, ?)
`);

// Проверяем существующие инструкции
const existingInstructions = db.prepare(
  'SELECT title FROM knowledge_instructions WHERE category_id = ?'
).all(toolsCategory.id) as { title: string }[];
const existingTitles = new Set(existingInstructions.map(i => i.title));

let added = 0;
instructions.forEach((instr, index) => {
  if (!existingTitles.has(instr.title)) {
    const id = uuidv4();
    insertInstruction.run(
      id,
      toolsCategory.id,
      instr.title,
      instr.content,
      JSON.stringify(instr.tags),
      index + 1,
      adminId,
      adminId
    );
    console.log(`✅ Добавлена инструкция: "${instr.title}"`);
    added++;
  } else {
    console.log(`ℹ️ Инструкция "${instr.title}" уже существует`);
  }
});

console.log(`\n🎉 Готово! Добавлено ${added} инструкций.`);
console.log('📖 Откройте Базу знаний → Общие инструкции → Инструменты');

