import Database, { Database as DatabaseType } from 'better-sqlite3';
import path from 'path';
import { v4 as uuidv4 } from 'uuid';

const dbPath = path.join(__dirname, '..', 'data', 'tracker.db');
const db: DatabaseType = new Database(dbPath);

// Включаем внешние ключи
db.pragma('foreign_keys = ON');

// Создаём таблицы
db.exec(`
  -- Таблица пользователей
  CREATE TABLE IF NOT EXISTS users (
    id TEXT PRIMARY KEY,
    username TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,
    full_name TEXT NOT NULL,
    role TEXT NOT NULL CHECK(role IN ('admin', 'buyer', 'webdev', 'creo_manager', 'buying_head', 'bizdev', 'creo_head', 'dev_head')),
    telegram_chat_id TEXT,
    telegram_username TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица партнёрок (ПП)
  CREATE TABLE IF NOT EXISTS partners (
    id TEXT PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    description TEXT,
    website TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_by TEXT REFERENCES users(id)
  );

  -- Таблица офферов
  CREATE TABLE IF NOT EXISTS offers (
    id TEXT PRIMARY KEY,
    partner_id TEXT NOT NULL REFERENCES partners(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    theme TEXT NOT NULL,
    partner_link TEXT,
    landing_price TEXT,
    promo_link TEXT,
    payout TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_by TEXT REFERENCES users(id)
  );

  -- Таблица тасков
  CREATE TABLE IF NOT EXISTS tasks (
    id TEXT PRIMARY KEY,
    task_number INTEGER,
    title TEXT NOT NULL,
    description TEXT,
    task_type TEXT NOT NULL,
    geo TEXT,
    department TEXT CHECK(department IN ('buying', 'creo', 'development')),
    customer_id TEXT NOT NULL REFERENCES users(id),
    executor_id TEXT NOT NULL REFERENCES users(id),
    deadline DATETIME NOT NULL,
    status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'in_progress', 'completed', 'cancelled')),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME
  );

  -- Таблица файлов
  CREATE TABLE IF NOT EXISTS task_files (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    original_name TEXT NOT NULL,
    stored_name TEXT NOT NULL,
    mime_type TEXT NOT NULL,
    size INTEGER NOT NULL,
    uploaded_by TEXT NOT NULL REFERENCES users(id),
    is_result INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица уведомлений
  CREATE TABLE IF NOT EXISTS notifications (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    type TEXT NOT NULL,
    title TEXT NOT NULL,
    message TEXT NOT NULL,
    task_id TEXT REFERENCES tasks(id) ON DELETE CASCADE,
    is_read INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица комментариев к задачам
  CREATE TABLE IF NOT EXISTS task_comments (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    message TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица отделов
  CREATE TABLE IF NOT EXISTS departments (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    code TEXT NOT NULL UNIQUE CHECK(code IN ('buying', 'creo', 'development')),
    head_id TEXT REFERENCES users(id),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица связи пользователей и отделов
  CREATE TABLE IF NOT EXISTS user_departments (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    department_id TEXT NOT NULL REFERENCES departments(id) ON DELETE CASCADE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, department_id)
  );

  -- Таблица руководителей отделов (многие-ко-многим)
  CREATE TABLE IF NOT EXISTS department_heads (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    department_id TEXT NOT NULL REFERENCES departments(id) ON DELETE CASCADE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, department_id)
  );

  -- Таблица категорий базы знаний
  CREATE TABLE IF NOT EXISTS knowledge_categories (
    id TEXT PRIMARY KEY,
    department_code TEXT NOT NULL CHECK(department_code IN ('buying', 'creo', 'development', 'general')),
    title TEXT NOT NULL,
    icon TEXT DEFAULT 'FileText',
    sort_order INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица инструкций базы знаний
  CREATE TABLE IF NOT EXISTS knowledge_instructions (
    id TEXT PRIMARY KEY,
    category_id TEXT NOT NULL REFERENCES knowledge_categories(id) ON DELETE CASCADE,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    tags TEXT,
    sort_order INTEGER DEFAULT 0,
    created_by TEXT REFERENCES users(id),
    updated_by TEXT REFERENCES users(id),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );

  -- Таблица дополнительных прав пользователей
  CREATE TABLE IF NOT EXISTS user_permissions (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    permission TEXT NOT NULL,
    granted_by TEXT REFERENCES users(id),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, permission)
  );

  -- Таблица достижений (справочник)
  CREATE TABLE IF NOT EXISTS achievements (
    id TEXT PRIMARY KEY,
    code TEXT UNIQUE NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    icon TEXT NOT NULL,
    category TEXT NOT NULL CHECK(category IN ('tasks', 'quality', 'speed', 'streak', 'special')),
    threshold INTEGER DEFAULT 1,
    sort_order INTEGER DEFAULT 0
  );

  -- Таблица полученных достижений пользователями
  CREATE TABLE IF NOT EXISTS user_achievements (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    achievement_id TEXT NOT NULL REFERENCES achievements(id) ON DELETE CASCADE,
    earned_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, achievement_id)
  );
`);

// Миграции
try {
  // Миграция users: обновляем CHECK constraint для новых ролей
  const checkInfo = db.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='users'").get() as { sql: string } | undefined;
  if (checkInfo && !checkInfo.sql.includes('dev_head')) {
    console.log('🔄 Миграция: обновление ролей пользователей...');
    db.exec(`
      -- Отключаем внешние ключи для миграции
      PRAGMA foreign_keys = OFF;
      
      -- Создаём новую таблицу с обновлённым constraint
      CREATE TABLE users_new (
        id TEXT PRIMARY KEY,
        username TEXT UNIQUE NOT NULL,
        password TEXT NOT NULL,
        full_name TEXT NOT NULL,
        role TEXT NOT NULL CHECK(role IN ('admin', 'buyer', 'webdev', 'creo_manager', 'buying_head', 'bizdev', 'creo_head', 'dev_head')),
        telegram_chat_id TEXT,
        telegram_username TEXT,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      );
      
      -- Копируем данные
      INSERT INTO users_new SELECT id, username, password, full_name, role, telegram_chat_id, telegram_username, created_at FROM users;
      
      -- Удаляем старую таблицу
      DROP TABLE users;
      
      -- Переименовываем новую
      ALTER TABLE users_new RENAME TO users;
      
      -- Включаем внешние ключи обратно
      PRAGMA foreign_keys = ON;
    `);
    console.log('✅ Миграция ролей завершена');
  }

  // Миграция users: telegram колонки
  const userColumns = db.prepare("PRAGMA table_info(users)").all() as Array<{ name: string }>;
  const userColumnNames = userColumns.map(c => c.name);
  
  if (!userColumnNames.includes('telegram_chat_id')) {
    db.exec('ALTER TABLE users ADD COLUMN telegram_chat_id TEXT');
  }
  if (!userColumnNames.includes('telegram_username')) {
    db.exec('ALTER TABLE users ADD COLUMN telegram_username TEXT');
  }

  // Миграция tasks: task_number и geo
  const taskColumns = db.prepare("PRAGMA table_info(tasks)").all() as Array<{ name: string }>;
  const taskColumnNames = taskColumns.map(c => c.name);
  
  if (!taskColumnNames.includes('task_number')) {
    db.exec('ALTER TABLE tasks ADD COLUMN task_number INTEGER');
    // Присваиваем номера существующим задачам
    const existingTasks = db.prepare('SELECT id FROM tasks ORDER BY created_at ASC').all() as Array<{ id: string }>;
    existingTasks.forEach((task, index) => {
      db.prepare('UPDATE tasks SET task_number = ? WHERE id = ?').run(index + 1, task.id);
    });
  }
  if (!taskColumnNames.includes('geo')) {
    db.exec('ALTER TABLE tasks ADD COLUMN geo TEXT');
  }

  // Миграция offers: geo колонка
  const offerColumns = db.prepare("PRAGMA table_info(offers)").all() as Array<{ name: string }>;
  const offerColumnNames = offerColumns.map(c => c.name);
  
  if (!offerColumnNames.includes('geo')) {
    console.log('🔄 Миграция: добавление GEO к офферам...');
    db.exec('ALTER TABLE offers ADD COLUMN geo TEXT');
    console.log('✅ Миграция GEO для офферов завершена');
  }

  // Миграция offers: payment_type колонка (COD или SS)
  if (!offerColumnNames.includes('payment_type')) {
    console.log('🔄 Миграция: добавление payment_type к офферам...');
    db.exec("ALTER TABLE offers ADD COLUMN payment_type TEXT CHECK(payment_type IN ('COD', 'SS'))");
    console.log('✅ Миграция payment_type для офферов завершена');
  }

  // Миграция offers: garant колонка
  if (!offerColumnNames.includes('garant')) {
    console.log('🔄 Миграция: добавление garant к офферам...');
    db.exec("ALTER TABLE offers ADD COLUMN garant TEXT");
    console.log('✅ Миграция garant для офферов завершена');
  }

  // Миграция offers: cap колонка
  if (!offerColumnNames.includes('cap')) {
    console.log('🔄 Миграция: добавление cap к офферам...');
    db.exec("ALTER TABLE offers ADD COLUMN cap TEXT");
    console.log('✅ Миграция cap для офферов завершена');
  }

  // Миграция offers: comment колонка
  if (!offerColumnNames.includes('comment')) {
    console.log('🔄 Миграция: добавление comment к офферам...');
    db.exec("ALTER TABLE offers ADD COLUMN comment TEXT");
    console.log('✅ Миграция comment для офферов завершена');
  }

  // Миграция tasks: started_at колонка (когда задача взята в работу)
  if (!taskColumnNames.includes('started_at')) {
    console.log('🔄 Миграция: добавление started_at к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN started_at DATETIME");
    console.log('✅ Миграция started_at для задач завершена');
  }

  // Миграция tasks: priority колонка
  if (!taskColumnNames.includes('priority')) {
    console.log('🔄 Миграция: добавление приоритета к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN priority TEXT DEFAULT 'normal'");
    console.log('✅ Миграция приоритета для задач завершена');
  }

  // Миграция tasks: department колонка
  if (!taskColumnNames.includes('department')) {
    console.log('🔄 Миграция: добавление отдела к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN department TEXT CHECK(department IN ('buying', 'creo', 'development'))");
    console.log('✅ Миграция отдела для задач завершена');
  }

  // Миграция tasks: offer_id колонка
  if (!taskColumnNames.includes('offer_id')) {
    console.log('🔄 Миграция: добавление offer_id к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN offer_id TEXT REFERENCES offers(id)");
    console.log('✅ Миграция offer_id для задач завершена');
  }

  // Миграция tasks: rating колонка (оценка результата: bad, ok, top)
  if (!taskColumnNames.includes('rating')) {
    console.log('🔄 Миграция: добавление rating к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN rating TEXT CHECK(rating IN ('bad', 'ok', 'top'))");
    console.log('✅ Миграция rating для задач завершена');
  }

  // Миграция users: settings колонка для персонализации
  if (!userColumnNames.includes('settings')) {
    console.log('🔄 Миграция: добавление настроек персонализации...');
    db.exec("ALTER TABLE users ADD COLUMN settings TEXT");
    console.log('✅ Миграция настроек персонализации завершена');
  }

  // Миграция: создание начальных отделов
  const existingDepartments = db.prepare('SELECT COUNT(*) as count FROM departments').get() as { count: number };
  if (existingDepartments.count === 0) {
    console.log('🔄 Миграция: создание начальных отделов...');
    const { v4: uuidv4 } = require('uuid');
    db.prepare('INSERT INTO departments (id, name, code) VALUES (?, ?, ?)').run(uuidv4(), 'Баинг', 'buying');
    db.prepare('INSERT INTO departments (id, name, code) VALUES (?, ?, ?)').run(uuidv4(), 'Крео', 'creo');
    db.prepare('INSERT INTO departments (id, name, code) VALUES (?, ?, ?)').run(uuidv4(), 'Разработка', 'development');
    console.log('✅ Начальные отделы созданы');
  }

  // Миграция: создание начальных инструкций базы знаний
  const existingKnowledgeCategories = db.prepare('SELECT COUNT(*) as count FROM knowledge_categories').get() as { count: number };
  if (existingKnowledgeCategories.count === 0) {
    console.log('🔄 Миграция: создание начальных инструкций базы знаний...');
    const { v4: uuidv4 } = require('uuid');
    
    // Категория "Работа с офферами"
    const offersCategoryId = uuidv4();
    db.prepare('INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order) VALUES (?, ?, ?, ?, ?)').run(
      offersCategoryId, 'development', 'Работа с офферами', 'Globe', 1
    );

    // Инструкция "Заведение офферов"
    const offersInstructionContent = `# Заведение Офферов

При заведении Оффера (ровно как и переделывании Лендинга в Оффер) необходимо иметь информацию о том, какой **Продукт** и из какой **ПП** необходимо использовать.

---

## 1. Очистка Оффера

Оффер необходимо очистить от:

- [ ] Ссылок на сторонние ресурсы
- [ ] Лишних скриптов (как подключенных JS, так и в index)
- [ ] Кнопок в конце статьи (при наличии)

### Важно!
- Кнопки заменяем на **форму**
- Количество полей берём в референсном Оффере или в Шаблонном
- **Все лишние поля и/или скрипты — УДАЛЯЕМ, а не комментируем!**

---

## 2. Ссылки на главной странице

На главной странице Оффера **ВСЕ ссылки** должны вести в конец статьи к форме, а любое упоминание продукта должно быть релевантным.

---

## 3. Настройка формы

Форма может иметь разное количество полей заполнения, в зависимости от ПП, на которую будем запускаться.

### Особенности:
- Имена полей могут варьироваться от ПП к ПП
- Но всегда будут одинаковые от продукта к продукту **в рамках одной ПП**

### Пример:
Заводя/переделывая Оффер (к примеру OpenAff) — можно количество и \`name\` полей подглядеть в любом заведённом ранее OpenAff продукте и/или шаблонном.

---

## 4. Валидация полей

Валидацию полей **всегда** реализуем одинаково:

- Если проверку или требование вводимых символов/кодов города, текста в инпутах удобно вынести **ЗА пределы index** (например, в отдельный JS)
- ТО на всех Офферах именно в этом JS и держим валидацию
- Это нужно, чтобы не возникало бардака с формами и скриптами

### Цель:
Для удобства дублирования, переделывания, редактирования необходимо прийти к **общему формату формы** в Офферах.

### Исключения:
Подготовленные под нас Офферы от ПП (которые мы помечаем в нейминге \`DAFULT\`) — из этих Офферов мы можем брать только различные ID из \`send.php\`.

---

## 5. Сохранение Оффера

Новый Оффер сохраняем с неймингом:

\`\`\`
ПРОДУКТ [ГЕО][ПОДХОД][СЕЛЕБА]
\`\`\`

### Правила:
- Если Оффер **НЕ селебный** — блок с селебой не указываем
- Группу указываем \`TEST\`
- Указываем ПП в выпадающем списке «Партнерская сеть»
- Указываем ГЕО Оффера во вкладке «Настройки»

---

## 6. Финальная проверка

Проверяем Оффер на выполнение предыдущих пунктов:

- [ ] В Оффере должно быть упоминание только необходимого продукта
- [ ] Все ссылки должны вести в конец статьи к форме
- [ ] В Оффере НЕ должно быть подключенных НЕ ИСПОЛЬЗУЕМЫХ скриптов (например, незадействованный \`intlTelInput.js\`)
- [ ] Все стили Оффера НЕ нарушены
- [ ] Локализация Оффера релевантна

---

## 7. Завершение работы

1. Переводим Оффер в группу \`DONE\`
2. Отписываем таск-креатору ответом на сообщение, что Оффер готов к использованию

---

## 8. Действия таск-креатора

Таск-креатор, при получении Оффера:
1. Выполняет п.7 (проверку)
2. Даёт название подхода в Оффере (путём переименовывания Оффера в блоке \`[ПОДХОД]\`)`;

    db.prepare('INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order) VALUES (?, ?, ?, ?, ?, ?)').run(
      uuidv4(), offersCategoryId, 'Заведение офферов', offersInstructionContent, JSON.stringify(['оффер', 'заведение', 'форма', 'нейминг']), 1
    );

    // Категория "Отправка форм"
    const formsCategoryId = uuidv4();
    db.prepare('INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order) VALUES (?, ?, ?, ?, ?)').run(
      formsCategoryId, 'development', 'Отправка форм по партнёркам', 'Code', 2
    );

    // Категория "Создание лендингов"
    const landingsCategoryId = uuidv4();
    db.prepare('INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order) VALUES (?, ?, ?, ?, ?)').run(
      landingsCategoryId, 'development', 'Создание лендингов', 'FileText', 3
    );

    // === Категории для отдела Крео ===
    const creoCategoryId = uuidv4();
    db.prepare('INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order) VALUES (?, ?, ?, ?, ?)').run(
      creoCategoryId, 'creo', 'Работа с креативами', 'Globe', 1
    );

    // === Категории для отдела Баинга ===
    const buyingCategoryId = uuidv4();
    db.prepare('INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order) VALUES (?, ?, ?, ?, ?)').run(
      buyingCategoryId, 'buying', 'Работа с трафиком', 'Globe', 1
    );

    console.log('✅ Начальные инструкции базы знаний созданы');
  }

  // Миграция: is_result колонка для файлов задач
  const fileColumns = db.prepare("PRAGMA table_info(task_files)").all() as Array<{ name: string }>;
  const fileColumnNames = fileColumns.map(c => c.name);
  
  if (!fileColumnNames.includes('is_result')) {
    console.log('🔄 Миграция: добавление is_result к файлам задач...');
    db.exec("ALTER TABLE task_files ADD COLUMN is_result INTEGER DEFAULT 0");
    console.log('✅ Миграция is_result для файлов задач завершена');
  }

  // Миграция: parent_task_id колонка для подзадач
  if (!taskColumnNames.includes('parent_task_id')) {
    console.log('🔄 Миграция: добавление parent_task_id для подзадач...');
    db.exec("ALTER TABLE tasks ADD COLUMN parent_task_id TEXT REFERENCES tasks(id) ON DELETE CASCADE");
    console.log('✅ Миграция parent_task_id для подзадач завершена');
  }

  // Миграция: перенос head_id из departments в department_heads
  const existingHeads = db.prepare(`
    SELECT id as department_id, head_id FROM departments WHERE head_id IS NOT NULL
  `).all() as { department_id: string; head_id: string }[];

  for (const { department_id, head_id } of existingHeads) {
    const exists = db.prepare(`
      SELECT 1 FROM department_heads WHERE user_id = ? AND department_id = ?
    `).get(head_id, department_id);

    if (!exists) {
      console.log(`🔄 Миграция: перенос руководителя ${head_id} в department_heads...`);
      db.prepare(`
        INSERT INTO department_heads (id, user_id, department_id)
        VALUES (?, ?, ?)
      `).run(uuidv4(), head_id, department_id);
    }
  }

  // Миграция: создание инструкций "Руководство по работе с трекером" для каждого отдела
  const guideInstructionExists = db.prepare(`
    SELECT 1 FROM knowledge_instructions WHERE title = 'Руководство по работе с трекером'
  `).get();

  if (!guideInstructionExists) {
    console.log('🔄 Миграция: создание руководств по работе с трекером...');

    // Тексты инструкций для каждого отдела
    const devGuideContent = `# Руководство по работе с трекером
## Для отдела Разработки

---

## Ваша роль в системе

В зависимости от вашей должности, вам доступны различные функции:

### Веб-разработчик (webdev)
- Просмотр и выполнение задач, назначенных вам
- Просмотр офферов и информации по ним
- Загрузка результатов работы (файлов) к задачам
- Добавление комментариев к задачам
- Просмотр базы знаний отдела разработки
- Использование настроек персонализации

### Руководитель отдела разработки (dev_head)
Все возможности веб-разработчика, плюс:
- Просмотр дашборда "Мой отдел" со статистикой по сотрудникам
- Назначение задач сотрудникам отдела
- Редактирование и удаление задач отдела
- Оценка выполненных задач (Дно/Норм/Топ)
- Просмотр расширенной аналитики
- Редактирование базы знаний отдела
- Назначение сотрудников в отдел

---

## Основные разделы системы

### 📊 Дашборд
Главная страница с общей сводкой:
- Ваши активные задачи
- Ближайшие дедлайны
- Статистика выполнения

### 📦 Офферы
Список всех офферов в системе:
- Просмотр информации об офферах (продукт, ГЕО, ПП)
- Фильтрация по ПП и другим параметрам
- Поиск по названию

### ✅ Задачи
Ваш основной рабочий раздел:
- **Мои задачи** — задачи, где вы исполнитель
- **Созданные мной** — задачи, которые вы создали
- **Все задачи** — общий список (для руководителей)

#### Работа с задачей:
1. Открыть задачу кликом по строке
2. Изменить статус: Ожидает → В работе → Выполнено
3. Добавить комментарии по ходу работы
4. Загрузить файл-результат по завершении

#### Статусы задач:
- **Ожидает** — задача создана, ждёт начала работы
- **В работе** — вы приступили к выполнению
- **Выполнено** — работа завершена
- **Отменено** — задача отменена заказчиком

### 🏢 Отделы
Информация о структуре компании:
- Список отделов
- Сотрудники отделов
- Руководители

### 📚 База знаний
Инструкции и документация для вашего отдела:
- Инструкции по заведению офферов
- Работа с формами и партнёрками
- Технические гайды

### ⚙️ Настройки
Персональные настройки:
- Смена пароля
- Привязка Telegram для уведомлений
- Настройка темы оформления

---

## Уведомления

Система отправляет уведомления при:
- Назначении новой задачи
- Изменении статуса задачи
- Приближении дедлайна (за 24 часа)
- Просрочке задачи

Уведомления приходят:
- В колокольчик в правом верхнем углу
- В Telegram (если подключен)

---

## Горячие клавиши и советы

- Используйте поиск для быстрого нахождения задач
- Фильтры сохраняются между сессиями
- Клик по номеру задачи копирует ссылку на неё

---

## Нужна помощь?

Обратитесь к руководителю отдела или в базу знаний за дополнительной информацией.`;

    const creoGuideContent = `# Руководство по работе с трекером
## Для отдела Крео

---

## Ваша роль в системе

В зависимости от вашей должности, вам доступны различные функции:

### Крео менеджер (creo_manager)
- Просмотр и выполнение задач, назначенных вам
- Создание задач для других сотрудников
- Просмотр офферов и информации по ним
- Загрузка результатов работы (креативов) к задачам
- Добавление комментариев к задачам
- Использование инструментов (очистка метаданных, конвертер изображений)
- Просмотр базы знаний отдела крео
- Использование настроек персонализации

### Руководитель отдела крео (creo_head)
Все возможности крео менеджера, плюс:
- Просмотр дашборда "Мой отдел" со статистикой по сотрудникам
- Назначение задач сотрудникам отдела
- Редактирование и удаление задач отдела
- Оценка выполненных задач (Дно/Норм/Топ)
- Просмотр расширенной аналитики
- Редактирование базы знаний отдела
- Назначение сотрудников в отдел

---

## Основные разделы системы

### 📊 Дашборд
Главная страница с общей сводкой:
- Ваши активные задачи
- Ближайшие дедлайны
- Статистика выполнения

### 📦 Офферы
Список всех офферов в системе:
- Просмотр информации об офферах (продукт, ГЕО, подход)
- Фильтрация по ПП и другим параметрам
- Поиск по названию
- Здесь вы можете понять, какие продукты сейчас в работе

### ✅ Задачи
Ваш основной рабочий раздел:
- **Мои задачи** — задачи, где вы исполнитель
- **Созданные мной** — задачи, которые вы создали
- **Все задачи** — общий список (для руководителей)

#### Типы задач для крео:
- **Подготовить крео** — основной тип для создания креативов

#### Работа с задачей:
1. Открыть задачу кликом по строке
2. Изменить статус: Ожидает → В работе → Выполнено
3. Добавить комментарии по ходу работы
4. Загрузить готовые креативы как файл-результат

#### Статусы задач:
- **Ожидает** — задача создана, ждёт начала работы
- **В работе** — вы приступили к выполнению
- **Выполнено** — креативы готовы и загружены
- **Отменено** — задача отменена заказчиком

### 🏢 Отделы
Информация о структуре компании:
- Список отделов
- Сотрудники отделов
- Руководители

### 📚 База знаний
Инструкции и документация для вашего отдела:
- Гайды по созданию креативов
- Требования к форматам
- Лучшие практики

### 🔧 Инструменты
Полезные утилиты для работы:

#### Очистка метаданных
Удаление EXIF и других метаданных из изображений для безопасности:
- Загрузите изображение
- Получите очищенную версию без метаданных

#### Конвертер изображений
Конвертация изображений между форматами:
- Поддержка JPG, PNG, WebP, GIF
- Настройка качества сжатия

### ⚙️ Настройки
Персональные настройки:
- Смена пароля
- Привязка Telegram для уведомлений
- Настройка темы оформления

---

## Уведомления

Система отправляет уведомления при:
- Назначении новой задачи
- Изменении статуса задачи
- Приближении дедлайна (за 24 часа)
- Просрочке задачи

Уведомления приходят:
- В колокольчик в правом верхнем углу
- В Telegram (если подключен)

---

## Рекомендации по работе

1. **Всегда загружайте результат** — это подтверждение выполненной работы
2. **Используйте комментарии** — для уточнений и вопросов по задаче
3. **Следите за дедлайнами** — система напомнит, но лучше планировать заранее
4. **Очищайте метаданные** — перед отправкой креативов

---

## Нужна помощь?

Обратитесь к руководителю отдела или в базу знаний за дополнительной информацией.`;

    const buyingGuideContent = `# Руководство по работе с трекером
## Для отдела Баинга

---

## Ваша роль в системе

В зависимости от вашей должности, вам доступны различные функции:

### Байер (buyer)
- Просмотр и выполнение задач, назначенных вам
- Создание задач для отделов разработки и крео
- Просмотр офферов и информации по ним
- Добавление комментариев к задачам
- Использование инструментов (очистка метаданных, конвертер изображений)
- Просмотр базы знаний отдела баинга
- Использование настроек персонализации

### БизДев (bizdev)
Все возможности байера, плюс:
- Доступ к разделу "Партнёрки" (создание, редактирование)
- Создание офферов
- Просмотр расширенной аналитики
- Работа с партнёрскими сетями

### Руководитель отдела баинга (buying_head)
Все возможности байера и биздева, плюс:
- Просмотр дашборда "Мой отдел" со статистикой по сотрудникам
- Назначение задач сотрудникам отдела
- Редактирование и удаление задач отдела
- Оценка выполненных задач (Дно/Норм/Топ)
- Редактирование базы знаний отдела
- Назначение сотрудников в отдел

---

## Основные разделы системы

### 📊 Дашборд
Главная страница с общей сводкой:
- Ваши активные задачи
- Ближайшие дедлайны
- Статистика выполнения

### 🏢 Партнёрки (для bizdev и buying_head)
Управление партнёрскими сетями:
- Добавление новых ПП
- Редактирование информации о ПП
- Просмотр связанных офферов

### 📦 Офферы
Управление офферами:
- Создание новых офферов (bizdev, buying_head)
- Просмотр всех офферов в системе
- Фильтрация по ПП, ГЕО, тематике
- Привязка офферов к задачам

#### Создание оффера (для bizdev):
1. Нажмите "Добавить оффер"
2. Выберите ПП
3. Укажите название, тематику, ГЕО
4. Заполните ссылки и payout

### ✅ Задачи
Ваш основной рабочий раздел:
- **Мои задачи** — задачи, где вы исполнитель
- **Созданные мной** — задачи, которые вы создали для других отделов
- **Все задачи** — общий список

#### Создание задачи:
1. Нажмите "Новая задача"
2. Выберите тип задачи:
   - **Завести ленд** — для отдела разработки
   - **Подготовить крео** — для отдела крео
   - **Завести в Keitaro** — техническая настройка
   - **Другое** — прочие задачи
3. Укажите исполнителя, дедлайн, приоритет
4. Добавьте описание и прикрепите файлы (ТЗ)
5. Привяжите к офферу (если нужно)

#### Отслеживание задач:
- Следите за статусами созданных задач
- Оставляйте комментарии для уточнений
- Оценивайте результат после выполнения

### 📈 Аналитика (для bizdev и buying_head)
Расширенная статистика:
- Выполнение задач по отделам
- Средние сроки выполнения
- Статистика по исполнителям

### 🏢 Отделы
Информация о структуре компании:
- Список отделов и сотрудников
- Контакты руководителей

### 📚 База знаний
Инструкции и документация для вашего отдела:
- Работа с партнёрками
- Процессы заведения офферов
- Лучшие практики

### 🔧 Инструменты
Полезные утилиты:

#### Очистка метаданных
Удаление EXIF-данных из изображений

#### Конвертер изображений
Конвертация между форматами (JPG, PNG, WebP)

### ⚙️ Настройки
Персональные настройки:
- Смена пароля
- Привязка Telegram
- Тема оформления

---

## Рабочий процесс

### Типичный флоу запуска оффера:

1. **БизДев** получает новый продукт от ПП
2. **БизДев** создаёт оффер в системе
3. **Байер/БизДев** создаёт задачу для разработки: "Завести ленд"
4. **Байер/БизДев** создаёт задачу для крео: "Подготовить крео"
5. **Разработчик** заводит лендинг, меняет статус на "Выполнено"
6. **Крео менеджер** делает креативы, загружает результат
7. **Байер** оценивает результат и запускает в работу

---

## Уведомления

Система отправляет уведомления при:
- Изменении статуса созданных вами задач
- Выполнении задач
- Комментариях к вашим задачам

Уведомления приходят:
- В колокольчик в правом верхнем углу
- В Telegram (если подключен)

---

## Советы

1. **Чётко формулируйте ТЗ** — прикрепляйте референсы и детальные описания
2. **Указывайте правильные дедлайны** — учитывайте реальные сроки выполнения
3. **Используйте привязку к офферу** — это поможет отслеживать все задачи по продукту
4. **Ставьте приоритеты** — срочные задачи должны быть помечены как высокий приоритет

---

## Нужна помощь?

Обратитесь к руководителю отдела или в базу знаний за дополнительной информацией.`;

    const guides: Record<string, string> = {
      'development': devGuideContent,
      'creo': creoGuideContent,
      'buying': buyingGuideContent
    };

    const categoryTitle = 'Общая информация';
    const instructionTitle = 'Руководство по работе с трекером';

    for (const dept of ['development', 'creo', 'buying']) {
      // Находим или создаём категорию "Общая информация"
      let category = db.prepare(`
        SELECT id FROM knowledge_categories WHERE department_code = ? AND title = ?
      `).get(dept, categoryTitle) as { id: string } | undefined;

      if (!category) {
        const categoryId = uuidv4();
        db.prepare(`
          INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order)
          VALUES (?, ?, ?, 'BookOpen', 0)
        `).run(categoryId, dept, categoryTitle);
        category = { id: categoryId };
      }

      // Создаём инструкцию
      db.prepare(`
        INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order)
        VALUES (?, ?, ?, ?, ?, 0)
      `).run(
        uuidv4(),
        category.id,
        instructionTitle,
        guides[dept],
        JSON.stringify(['руководство', 'начало работы', 'гайд'])
      );
    }

    console.log('✅ Руководства по работе с трекером созданы для всех отделов');
  }

  // Миграция: добавление поддержки 'general' в knowledge_categories
  const knowledgeCategoriesInfo = db.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='knowledge_categories'").get() as { sql: string } | undefined;
  if (knowledgeCategoriesInfo && !knowledgeCategoriesInfo.sql.includes('general')) {
    console.log('🔄 Миграция: добавление поддержки общих инструкций...');
    db.exec(`
      PRAGMA foreign_keys = OFF;
      
      CREATE TABLE knowledge_categories_new (
        id TEXT PRIMARY KEY,
        department_code TEXT NOT NULL CHECK(department_code IN ('buying', 'creo', 'development', 'general')),
        title TEXT NOT NULL,
        icon TEXT DEFAULT 'FileText',
        sort_order INTEGER DEFAULT 0,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      );
      
      INSERT INTO knowledge_categories_new SELECT * FROM knowledge_categories;
      DROP TABLE knowledge_categories;
      ALTER TABLE knowledge_categories_new RENAME TO knowledge_categories;
      
      PRAGMA foreign_keys = ON;
    `);
    console.log('✅ Миграция общих инструкций завершена');
  }

  // Миграция: создание начальных общих инструкций
  const existingGeneralCategories = db.prepare(`
    SELECT COUNT(*) as count FROM knowledge_categories WHERE department_code = 'general'
  `).get() as { count: number };

  if (existingGeneralCategories.count === 0) {
    console.log('🔄 Миграция: создание общих инструкций...');

    // Категория "Общие правила работы"
    const rulesCategoryId = uuidv4();
    db.prepare(`
      INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order)
      VALUES (?, 'general', 'Общие правила работы', 'BookOpen', 1)
    `).run(rulesCategoryId);

    // Инструкция: Общие правила работы в компании
    const rulesContent = `# Общие правила работы в компании

---

## 🎯 Наша миссия

Мы создаём качественные продукты для арбитража трафика, помогая нашим клиентам достигать максимальных результатов.

---

## 📋 Основные принципы работы

### 1. Качество превыше скорости
- Лучше потратить больше времени на качественную работу, чем переделывать плохую
- Всегда проверяйте результат перед сдачей
- Если не уверены — спросите у коллег или руководителя

### 2. Коммуникация — ключ к успеху
- Не бойтесь задавать вопросы
- Сообщайте о проблемах сразу, не замалчивайте
- Держите коллег в курсе статуса вашей работы

### 3. Дедлайны — святое
- Если понимаете, что не успеваете — сообщите заранее
- Планируйте работу с запасом времени
- Расставляйте приоритеты правильно

### 4. Ответственность
- Вы отвечаете за свою работу от начала до конца
- Не перекладывайте ответственность на других
- Признавайте ошибки и исправляйте их

---

## 💬 Правила коммуникации

### Рабочие чаты
- Пишите по делу, без флуда
- Используйте треды для обсуждений
- Отмечайте нужных коллег через @

### Задачи в трекере
- Все задачи ведутся в трекере
- Не принимайте задачи "на словах" — просите завести в систему
- Комментируйте ход работы в задаче

### Встречи
- Приходите вовремя
- Готовьтесь заранее
- Включайте камеру на созвонах

---

## ⏰ Рабочий режим

### Гибкий график
- Основное рабочее время: 10:00 — 19:00
- Обязательное присутствие онлайн: 11:00 — 17:00
- Обед: 1 час в удобное время

### Отпуска и больничные
- Отпуск согласовывайте минимум за 2 недели
- О больничном сообщайте руководителю в первый день
- Все отсутствия фиксируйте в HR-системе

---

## 🔒 Безопасность

### Конфиденциальность
- Не обсуждайте рабочие вопросы в публичных местах
- Не делитесь доступами с посторонними
- Используйте VPN при работе с чувствительными данными

### Пароли и доступы
- Используйте сложные уникальные пароли
- Включите 2FA везде, где возможно
- Не храните пароли в открытом виде

---

## 🎓 Развитие

- Участвуйте во внутренних обучениях
- Делитесь знаниями с коллегами
- Следите за трендами в своей области

---

## ❓ Нужна помощь?

- **Технические вопросы** — руководитель отдела
- **HR вопросы** — HR-отдел
- **Административные вопросы** — администратор`;

    db.prepare(`
      INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order)
      VALUES (?, ?, 'Правила работы в компании', ?, ?, 1)
    `).run(uuidv4(), rulesCategoryId, rulesContent, JSON.stringify(['правила', 'работа', 'общие']));

    // Категория "Работа с трекером"
    const trackerCategoryId = uuidv4();
    db.prepare(`
      INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order)
      VALUES (?, 'general', 'Работа с трекером', 'FileText', 2)
    `).run(trackerCategoryId);

    // Инструкция: Как работать с задачами
    const tasksContent = `# Как работать с задачами

---

## 📝 Создание задачи

### Когда создавать задачу?
- Любая работа, требующая более 15 минут
- Все запросы от других отделов
- Регулярные и повторяющиеся задачи

### Как правильно создать задачу?

1. **Название** — кратко и понятно
   - ✅ "Завести лендинг для оффера Vigrex [IT]"
   - ❌ "Сделать работу"

2. **Описание** — максимум деталей
   - Что нужно сделать
   - Какие материалы использовать
   - Ожидаемый результат
   - Ссылки и референсы

3. **Тип задачи** — выбирайте правильный тип
   - \`Завести ленд\` — для создания лендингов
   - \`Подготовить крео\` — для создания креативов
   - \`Завести в Keitaro\` — для технической настройки
   - \`Другое\` — для прочих задач

4. **Приоритет**
   - 🔴 Высокий — срочно, горит
   - 🟡 Обычный — стандартный приоритет
   - 🟢 Низкий — когда будет время

5. **Дедлайн** — ставьте реальные сроки
   - Учитывайте загрузку исполнителя
   - Оставляйте запас на проверку

6. **Исполнитель** — кто будет делать
   - Если не знаете — спросите руководителя отдела

---

## 🔄 Статусы задач

| Статус | Что означает |
|--------|--------------|
| ⏳ Ожидает | Задача создана, ждёт начала работы |
| 🔨 В работе | Исполнитель приступил к выполнению |
| ✅ Выполнено | Работа завершена |
| ❌ Отменено | Задача отменена |

---

## 💬 Комментарии

Используйте комментарии для:
- Уточнения деталей задачи
- Информирования о прогрессе
- Вопросов исполнителю/заказчику
- Обсуждения изменений

**Важно:** Все обсуждения по задаче — в комментариях, а не в личных сообщениях!

---

## 📎 Файлы

### Прикрепление файлов
- ТЗ и референсы — при создании задачи
- Результаты работы — при завершении (отметьте как "Файл-результат")

### Форматы файлов
- Изображения: JPG, PNG, WebP, GIF
- Документы: PDF, ZIP
- Максимальный размер: 50 MB

---

## ⭐ Оценка задач

После выполнения заказчик может оценить результат:

| Оценка | Значение |
|--------|----------|
| 👎 Дно | Работа требует серьёзных исправлений |
| 👌 Норм | Работа выполнена качественно |
| 🔥 Топ | Отличная работа, выше ожиданий |

---

## 🔔 Уведомления

Вы получите уведомление:
- При назначении новой задачи
- При изменении статуса
- При новом комментарии
- При приближении дедлайна (за 24 часа)
- При просрочке задачи

**Совет:** Подключите Telegram для мгновенных уведомлений!`;

    db.prepare(`
      INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order)
      VALUES (?, ?, 'Как работать с задачами', ?, ?, 1)
    `).run(uuidv4(), trackerCategoryId, tasksContent, JSON.stringify(['задачи', 'трекер', 'статусы']));

    // Инструкция: Горячие клавиши и советы
    const tipsContent = `# Горячие клавиши и полезные советы

---

## ⌨️ Горячие клавиши

### Навигация
| Клавиша | Действие |
|---------|----------|
| \`Ctrl/Cmd + K\` | Открыть командную палитру |
| \`Ctrl/Cmd + /\` | Показать все горячие клавиши |
| \`G\` затем \`D\` | Перейти на Дашборд |
| \`G\` затем \`T\` | Перейти в Задачи |
| \`G\` затем \`O\` | Перейти в Офферы |
| \`G\` затем \`K\` | Перейти в Базу знаний |

### Задачи
| Клавиша | Действие |
|---------|----------|
| \`N\` | Создать новую задачу |
| \`/\` | Фокус на поиск |
| \`Esc\` | Закрыть модальное окно |

---

## 💡 Полезные советы

### Продуктивность
1. **Начинайте день с просмотра задач** — оцените приоритеты
2. **Не переключайтесь между задачами** — закончите одну, начните другую
3. **Используйте фильтры** — они сохраняются между сессиями

### Организация
1. **Все задачи — в трекере** — не держите в голове
2. **Обновляйте статусы сразу** — коллеги видят актуальную информацию
3. **Пишите подробные комментарии** — это поможет и вам, и другим

### Коммуникация
1. **Отвечайте на комментарии в течение дня** — не оставляйте без внимания
2. **Если вопрос срочный** — напишите в чат, а не только в задачу
3. **Благодарите коллег** — это важно!

---

## 🎨 Персонализация

В настройках вы можете:
- Изменить тему оформления
- Загрузить свой фон
- Настроить уведомления
- Подключить Telegram

---

## 📱 Telegram-уведомления

### Как подключить?
1. Перейдите в **Настройки** → **Telegram**
2. Нажмите "Получить ссылку"
3. Перейдите по ссылке и нажмите Start в боте
4. Готово! Уведомления будут приходить в Telegram

### Что приходит в Telegram?
- Новые назначенные задачи
- Изменения статуса ваших задач
- Напоминания о дедлайнах
- Комментарии к вашим задачам`;

    db.prepare(`
      INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order)
      VALUES (?, ?, 'Горячие клавиши и советы', ?, ?, 2)
    `).run(uuidv4(), trackerCategoryId, tipsContent, JSON.stringify(['горячие клавиши', 'советы', 'продуктивность']));

    // Категория "Инструменты"
    const toolsCategoryId = uuidv4();
    db.prepare(`
      INSERT INTO knowledge_categories (id, department_code, title, icon, sort_order)
      VALUES (?, 'general', 'Инструменты', 'Code', 3)
    `).run(toolsCategoryId);

    // Инструкция: Очистка метаданных
    const metadataContent = `# Очистка метаданных изображений

---

## ❓ Зачем это нужно?

Каждое фото или изображение содержит **метаданные (EXIF)** — скрытую информацию:
- Дата и время съёмки
- Модель камеры/телефона
- GPS-координаты места съёмки
- Имя автора
- Программа редактирования

**Эта информация может раскрыть конфиденциальные данные!**

---

## 🛠 Как использовать инструмент?

1. Перейдите в **Инструменты** → **Очистка метаданных**
2. Загрузите изображение (или несколько)
3. Нажмите "Очистить"
4. Скачайте очищенный файл

### Поддерживаемые форматы
- JPEG / JPG
- PNG
- WebP
- GIF

---

## ⚠️ Когда обязательно очищать?

- Перед загрузкой креативов в рекламные сети
- Перед отправкой клиентам
- При публикации на внешних ресурсах
- При работе с конфиденциальными изображениями

---

## 💡 Советы

1. **Очищайте сразу после создания** — сделайте это привычкой
2. **Проверяйте пакетно** — инструмент поддерживает несколько файлов
3. **Сохраняйте оригиналы** — на случай, если понадобятся метаданные`;

    db.prepare(`
      INSERT INTO knowledge_instructions (id, category_id, title, content, tags, sort_order)
      VALUES (?, ?, 'Очистка метаданных', ?, ?, 1)
    `).run(uuidv4(), toolsCategoryId, metadataContent, JSON.stringify(['метаданные', 'exif', 'безопасность']));

    console.log('✅ Общие инструкции созданы');
  }

  // Миграция: создание начальных достижений
  const existingAchievements = db.prepare('SELECT COUNT(*) as count FROM achievements').get() as { count: number };
  if (existingAchievements.count < 30) {
    console.log('🔄 Миграция: создание/обновление достижений...');
    
    // Очищаем старые достижения (без потери полученных пользователями)
    const earnedAchievements = db.prepare('SELECT DISTINCT achievement_id FROM user_achievements').all() as { achievement_id: string }[];
    const earnedIds = earnedAchievements.map(a => a.achievement_id);
    
    // Удаляем только те достижения, которые никто не получил
    db.prepare(`DELETE FROM achievements WHERE id NOT IN (${earnedIds.length > 0 ? earnedIds.map(() => '?').join(',') : "''"})`)
      .run(...earnedIds);
    
    const achievements = [
      // === По количеству задач ===
      { code: 'tasks_5', name: 'Новичок', description: 'Выполнить 5 задач', icon: '🌱', category: 'tasks', threshold: 5, sort_order: 1 },
      { code: 'tasks_25', name: 'Работяга', description: 'Выполнить 25 задач', icon: '💪', category: 'tasks', threshold: 25, sort_order: 2 },
      { code: 'tasks_50', name: 'Машина', description: 'Выполнить 50 задач', icon: '🔥', category: 'tasks', threshold: 50, sort_order: 3 },
      { code: 'tasks_100', name: 'Легенда', description: 'Выполнить 100 задач', icon: '👑', category: 'tasks', threshold: 100, sort_order: 4 },
      { code: 'tasks_250', name: 'Титан', description: 'Выполнить 250 задач', icon: '🏆', category: 'tasks', threshold: 250, sort_order: 5 },
      { code: 'tasks_500', name: 'Богатырь', description: 'Выполнить 500 задач', icon: '🌟', category: 'tasks', threshold: 500, sort_order: 6 },
      { code: 'tasks_1000', name: 'Бессмертный', description: 'Выполнить 1000 задач', icon: '💎', category: 'tasks', threshold: 1000, sort_order: 7 },
      
      // === По качеству (рейтинг "Топ") ===
      { code: 'quality_1', name: 'Первая звезда', description: 'Получить первый рейтинг "Топ"', icon: '⭐', category: 'quality', threshold: 1, sort_order: 1 },
      { code: 'quality_10', name: 'Звёздный', description: '10 задач с рейтингом "Топ"', icon: '🌟', category: 'quality', threshold: 10, sort_order: 2 },
      { code: 'quality_25', name: 'Бриллиант', description: '25 задач с рейтингом "Топ"', icon: '💎', category: 'quality', threshold: 25, sort_order: 3 },
      { code: 'quality_50', name: 'Перфекционист', description: '50 задач с рейтингом "Топ"', icon: '🎖️', category: 'quality', threshold: 50, sort_order: 4 },
      { code: 'quality_100', name: 'Безупречный', description: '100 задач с рейтингом "Топ"', icon: '👑', category: 'quality', threshold: 100, sort_order: 5 },
      
      // === По скорости (досрочно) ===
      { code: 'speed_5', name: 'Спринтер', description: '5 задач досрочно', icon: '⚡', category: 'speed', threshold: 5, sort_order: 1 },
      { code: 'speed_25', name: 'Ракета', description: '25 задач досрочно', icon: '🚀', category: 'speed', threshold: 25, sort_order: 2 },
      { code: 'speed_50', name: 'Молния', description: '50 задач досрочно', icon: '💨', category: 'speed', threshold: 50, sort_order: 3 },
      { code: 'speed_100', name: 'Формула-1', description: '100 задач досрочно', icon: '🏎️', category: 'speed', threshold: 100, sort_order: 4 },
      
      // === По стрику (только рабочие дни пн-пт) ===
      { code: 'streak_7', name: 'Неделя огня', description: '7 рабочих дней подряд с выполненными задачами', icon: '📅', category: 'streak', threshold: 7, sort_order: 1 },
      { code: 'streak_30', name: 'Месяц в ударе', description: '30 рабочих дней стрик', icon: '🔥', category: 'streak', threshold: 30, sort_order: 2 },
      { code: 'streak_100', name: 'Неостановимый', description: '100 рабочих дней стрик', icon: '💪', category: 'streak', threshold: 100, sort_order: 3 },
      { code: 'streak_365', name: 'Железная воля', description: '365 рабочих дней стрик', icon: '🏆', category: 'streak', threshold: 365, sort_order: 4 },
      
      // === Специальные ===
      { code: 'first_task', name: 'Первый шаг', description: 'Выполнить первую задачу', icon: '🎯', category: 'special', threshold: 1, sort_order: 1 },
      { code: 'veteran_year', name: 'Ветеран', description: '1 год в системе', icon: '🎂', category: 'special', threshold: 365, sort_order: 2 },
      { code: 'veteran_2years', name: 'Старожил', description: '2 года в системе', icon: '🎊', category: 'special', threshold: 730, sort_order: 3 },
      { code: 'early_bird', name: 'Ранняя пташка', description: '10 задач выполнено до 9:00', icon: '🌅', category: 'special', threshold: 10, sort_order: 4 },
      { code: 'night_owl', name: 'Полуночник', description: '10 задач выполнено после 22:00', icon: '🌙', category: 'special', threshold: 10, sort_order: 5 },
      { code: 'commentator', name: 'Комментатор', description: 'Оставить 50 комментариев', icon: '📝', category: 'special', threshold: 50, sort_order: 6 },
      { code: 'team_player', name: 'Командный игрок', description: 'Участие в 10 подзадачах', icon: '🤝', category: 'special', threshold: 10, sort_order: 7 },
      { code: 'mentor', name: 'Наставник', description: 'Поставить 5 оценок "Топ" как заказчик', icon: '🎓', category: 'special', threshold: 5, sort_order: 8 },
      
      // === Отделные ===
      { code: 'creo_master', name: 'Мастер крео', description: '50 задач в отделе Крео', icon: '🎨', category: 'special', threshold: 50, sort_order: 10 },
      { code: 'dev_guru', name: 'Гуру разработки', description: '50 задач в отделе Разработки', icon: '💻', category: 'special', threshold: 50, sort_order: 11 },
      { code: 'buying_king', name: 'Король баинга', description: '50 задач в отделе Баинга', icon: '📈', category: 'special', threshold: 50, sort_order: 12 },
      
      // === Редкие/Секретные ===
      { code: 'unicorn', name: 'Единорог', description: '10 задач "Топ" подряд', icon: '🦄', category: 'special', threshold: 10, sort_order: 20 },
      { code: 'marathon', name: 'Марафонец', description: '20 задач за одну неделю', icon: '🏃', category: 'special', threshold: 20, sort_order: 21 },
      { code: 'universal', name: 'Универсал', description: 'Задачи во всех типах', icon: '🎪', category: 'special', threshold: 5, sort_order: 22 },
      { code: 'prophet', name: 'Провидец', description: '5 задач выполнены ровно в дедлайн', icon: '🔮', category: 'special', threshold: 5, sort_order: 23 },
      { code: 'weekend_warrior', name: 'Воин выходного дня', description: 'Выполнить задачу в субботу или воскресенье', icon: '🦸', category: 'special', threshold: 1, sort_order: 24 },
    ];

    const insertAchievement = db.prepare(`
      INSERT OR REPLACE INTO achievements (id, code, name, description, icon, category, threshold, sort_order)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    `);

    for (const a of achievements) {
      // Проверяем, есть ли уже такое достижение
      const existing = db.prepare('SELECT id FROM achievements WHERE code = ?').get(a.code) as { id: string } | undefined;
      const id = existing?.id || uuidv4();
      insertAchievement.run(id, a.code, a.name, a.description, a.icon, a.category, a.threshold, a.sort_order);
    }

    console.log('✅ Достижения созданы/обновлены');
  }

  // Миграция tasks: viewed_at колонка (время первого просмотра исполнителем)
  if (!taskColumnNames.includes('viewed_at')) {
    console.log('🔄 Миграция: добавление viewed_at к задачам...');
    db.exec("ALTER TABLE tasks ADD COLUMN viewed_at DATETIME");
    console.log('✅ Миграция viewed_at для задач завершена');
  }

  // Миграция: обновление описаний стриков (рабочие дни вместо обычных)
  const streakDescCheck = db.prepare("SELECT description FROM achievements WHERE code = 'streak_7'").get() as { description: string } | undefined;
  if (streakDescCheck && !streakDescCheck.description.includes('рабочих')) {
    console.log('🔄 Миграция: обновление описаний стриков на рабочие дни...');
    
    db.prepare("UPDATE achievements SET description = '7 рабочих дней подряд с выполненными задачами' WHERE code = 'streak_7'").run();
    db.prepare("UPDATE achievements SET description = '30 рабочих дней стрик' WHERE code = 'streak_30'").run();
    db.prepare("UPDATE achievements SET description = '100 рабочих дней стрик' WHERE code = 'streak_100'").run();
    db.prepare("UPDATE achievements SET description = '365 рабочих дней стрик' WHERE code = 'streak_365'").run();
    
    console.log('✅ Описания стриков обновлены');
  }
} catch (e) {
  console.error('Migration error:', e);
}

// Функция для получения следующего номера задачи
export function getNextTaskNumber(): number {
  const result = db.prepare('SELECT MAX(task_number) as max_num FROM tasks').get() as { max_num: number | null };
  return (result.max_num || 0) + 1;
}

export default db;

