# Offer Tracker - Таск-трекер для арбитражников

Веб-приложение для управления партнёрками, офферами и задачами команды арбитражников.

## 🚀 Возможности

- **Три роли пользователей:** Админ, Байер, Веб-разработчик
- **Управление партнёрками (ПП):** LemonAd, ArbitPro, Sl-CPA и другие
- **Каталог офферов:** название, тематика, ссылки, цены, ставки
- **Таск-трекер:** создание задач с дедлайнами, назначение исполнителей

## 📋 Права доступа

| Действие | Админ | Байер | Веб-разраб |
|----------|-------|-------|------------|
| Создание ПП/офферов | ✅ | ✅ | ❌ |
| Удаление ПП/офферов | ✅ | ❌ | ❌ |
| Создание задач | ✅ | ✅ | ✅ |
| Управление пользователями | ✅ | ❌ | ❌ |

## 🛠 Технологии

**Backend:**
- Node.js + Express
- TypeScript
- SQLite (better-sqlite3)
- JWT авторизация

**Frontend:**
- React 18 + TypeScript
- Vite
- Tailwind CSS
- TanStack Query (React Query)
- Zustand
- React Router

## 📦 Установка

### 1. Клонирование и установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 2. Инициализация базы данных

```bash
cd backend
npm run seed
```

Это создаст тестовых пользователей:
- **Админ:** admin / admin123
- **Байер:** buyer1 / buyer123
- **Веб-разраб:** webdev1 / webdev123

### 3. Запуск

```bash
# Терминал 1 - Backend
cd backend
npm run dev

# Терминал 2 - Frontend
cd frontend
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

## 📁 Структура проекта

```
├── backend/
│   ├── src/
│   │   ├── routes/        # API endpoints
│   │   ├── middleware/    # Auth middleware
│   │   ├── database.ts    # SQLite setup
│   │   ├── types.ts       # TypeScript types
│   │   └── index.ts       # Express app
│   └── data/              # SQLite database
│
└── frontend/
    ├── src/
    │   ├── api/           # API client
    │   ├── components/    # React components
    │   ├── pages/         # Page components
    │   ├── store/         # Zustand store
    │   └── types/         # TypeScript types
    └── index.html
```

## 🎨 Типы задач

- Завести ленд
- Подготовить крео
- Завести в Keitaro
- Завести партнёра
- Другое

## 📝 API Endpoints

### Auth
- `POST /api/auth/login` - Авторизация
- `POST /api/auth/register` - Регистрация (только админ)
- `GET /api/auth/me` - Текущий пользователь
- `GET /api/auth/users` - Список пользователей

### Partners
- `GET /api/partners` - Список партнёрок
- `POST /api/partners` - Создать партнёрку
- `PUT /api/partners/:id` - Обновить партнёрку
- `DELETE /api/partners/:id` - Удалить партнёрку

### Offers
- `GET /api/offers` - Список офферов
- `POST /api/offers` - Создать оффер
- `PUT /api/offers/:id` - Обновить оффер
- `DELETE /api/offers/:id` - Удалить оффер

### Tasks
- `GET /api/tasks` - Список задач
- `GET /api/tasks/my` - Мои задачи
- `POST /api/tasks` - Создать задачу
- `PATCH /api/tasks/:id/status` - Обновить статус
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу

